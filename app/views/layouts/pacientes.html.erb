<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <title><%= h(yield(:title) || "Untitled") %></title>
	
	<%= stylesheet_link_tag "mbContainer","pacientes" %>

	<%= javascript_include_tag "prototype", "effects" %>
	<%= javascript_include_tag "jquery","jquery-ui.min","jquery.metadata","mbContainer","pacientes" %>

<%= yield(:head) %>
  </head>
  <body>
  <div class="wrapper">
  
    <div id="container" class="containerPlus draggable resizable {buttons:'m,i,c', icon:'restore.png', skin:'black', height:'500', width:'600',iconized:'false', dock:'dock', title:'Pacientes'}" style="position:fixed;top:130px;left:400px">
    
    <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <%- end -%>
      
      <%- if show_title? -%>
        <h1><%=h yield(:title) %></h1>
      <%- end -%>
      
      <%= yield %>
    </div>

    </div>

  <div id="dock"></div>
  <button id="open" onclick="$('.containerPlus').mb_open(); $(this).fadeOut();" style="display:none;">Abre janelas fechadas</button>

</body>
</html>
